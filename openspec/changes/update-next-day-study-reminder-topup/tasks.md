## 1. 授权日期状态

- [x] 1.1 扩展 `SubscribeMessageGrant` 的明日排队字段，保持旧记录兼容。
- [x] 1.2 在订阅状态中返回当前和排队日期，并判断指定期次的明日是否可请求。
- [x] 1.3 将明日通知授权保存改为按期次和日期幂等写入当前或排队槽。
- [x] 1.4 确保新请求被拒绝时不清空既有待发授权。

## 2. 小程序两个入口

- [x] 2.1 提供仅补充 `next_day_study_reminder` 的共用方法。
- [x] 2.2 首页 `handleJoinMeeting` 在跳转前尝试补充，同时覆盖普通按钮和时间弹窗。
- [x] 2.3 课程详情 `handleImmersiveReading` 在跳转前尝试补充。
- [x] 2.4 授权拒绝、禁用或失败时保持原晨读跳转。

## 3. 05:55 发送与并发保护

- [x] 3.1 将计划时间、主 cron、运行日志和前端文案统一改为 05:55。
- [x] 3.2 对到期项增加原子领取和 `sourceId` 成功投递幂等校验。
- [x] 3.3 发送成功后原子提升排队项，无排队项时才清空。
- [x] 3.4 06:00 起保留最多一次可重试错误投递，且不与主发并发重复。

## 4. 测试与验证

- [x] 4.1 补充订阅自动补量、首页和课程详情小程序单测。
- [x] 4.2 补充授权记录、日期队列、成功提升、拒绝保留和越界场景后端单测。
- [x] 4.3 覆盖 05:55 主发、06:00 重试、主任务重叠和幂等跳过。
- [x] 4.4 运行与改动相关的最小小程序与后端测试集，记录验证结果。

